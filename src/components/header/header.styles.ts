import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
    container: {
        width: '100%',
        backgroundColor: 'white',
        height: 50,
      },
      subContainer: {
        flexDirection: 'row',
      },
      backButton: {
        marginTop: 10,
        paddingHorizontal: 5
      }
})