import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
    inputTextLayout: {
        borderColor: '#304FFE',
        height: 58,
        borderWidth: 1,
        paddingHorizontal: 10,
        borderRadius: 10,
        paddingTop: 5,
        marginTop: 10,
      },
      input: {
        color: '#000000',
      },
})